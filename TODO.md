# TODO

---

# QA

## 项目动机与定义

### *你在简历里说 NovelSum、DistSum 等几何稀疏性指标容易高估离群噪声。你能具体解释一下，“真正的多样性”和“几何空间里的离群点”为什么不能等价吗？*

不能等价的核心原因是：**几何距离只说明样本在 embedding 空间里“远”，但不说明它对指令微调是否提供了有效语义增量**。

我会把两者区分成这样：

**真正的多样性**指的是数据覆盖了更多有效的任务意图、能力需求、表达方式和推理模式，比如问答、代码、数学、多轮对话、复杂指令遵循等。这类样本虽然彼此语义不同，但仍然属于目标指令分布的一部分，能帮助模型学到更广的指令响应能力。

而**几何空间里的离群点**只是 embedding 上远离已有样本。它可能有三种情况：

1. 它确实是新的有效语义模式，这是我们想要的多样性。
2. 它是低质量噪声，比如语法错误、乱码、格式异常、过短或无意义内容。它在 embedding 空间里可能很“独特”，但不会提升模型能力。
3. 它是分布外样本，比如文本本身质量不错，但和目标指令微调分布不一致。它也可能很远，但这种“远”更多是分布偏移，不一定是有用多样性。

NovelSum、DistSum 这类方法的问题在于，它们把距离、稀疏性或局部密度当成信息价值的主要代理。这样会天然偏好远离密集区域的点。但在高维语义空间里，远离中心既可能代表新意图，也可能代表异常点。只看几何距离无法区分这两者。

所以我们在 SCS 里引入了两个约束：

- **生成概率**：用 backbone 对样本的 token-level likelihood 判断文本是否像自然、合理的指令数据，用来压制低质量的 intrinsic noise。
- **语义簇内中心距离权重**：样本如果在某个语义簇边缘，说明它对该簇核心语义的凝聚贡献弱，因此降低它的权重，用来压制 extrinsic distributional noise。

因此我的观点不是“离群点一定没价值”，而是：**离群本身不是多样性的充分条件**。有效多样性应该同时满足“语义上提供新覆盖”和“质量、分布上仍然可信”。SCS 的设计就是把几何稀疏性重新约束到语义凝聚和生成概率这两个维度里。

### *SCS 最后用熵来建模多样性。为什么不用簇数量、平均簇间距离、或者簇大小方差？熵在这里表达了什么？*

我选择熵，是因为我想衡量的不是“有多少个簇”，也不是“簇之间离得多远”，而是一个数据集的有效语义贡献是不是分布得足够均衡。

簇数量只能说明语义类别的个数，但它忽略了分布。比如两个数据集都有 20 个簇，一个数据集大部分样本都集中在两三个簇里，另一个数据集在各个簇上比较均匀，它们的多样性显然不一样，但簇数量看不出来。

平均簇间距离的问题是，它仍然是几何视角。距离远不一定代表有价值，可能只是分布外样本或者噪声。我们这篇工作的出发点就是不能把几何稀疏直接等价为有效多样性。

簇大小方差能看数量是否均衡，但它也只看样本个数，不看样本质量和簇内代表性。一个簇里样本很多，但如果低质量样本多，或者很多样本都在簇边缘，对微调的有效贡献其实没那么高。

熵更适合的地方在于，它可以描述“有效语义质量”在不同簇之间的分布状态。如果高质量、代表性的样本覆盖了多个语义簇，而且分布比较均衡，熵就会高；如果有效样本集中在少数几个语义簇，熵就会低。

所以 SCS 里的熵不是直接对原始样本数量算，而是对经过生成概率和空间凝聚权重修正后的簇级贡献分布算。它表达的是数据集在目标模型语义空间里的有效覆盖程度和均衡程度。

### *如果一个样本语法很流畅、生成概率也很高，但主题和目标指令数据分布完全不相关，SCS 会怎么处理它？为什么？*
### *你论文里把噪声分成 intrinsic noise 和 extrinsic distributional noise。请分别举一个指令微调数据里的例子，并说明 SCS 分别靠哪个模块抑制它们。*

## 方法细节

### *生成概率在你的方法里代表什么？为什么它可以近似文本的 intrinsic quality？这个假设在哪些情况下可能失效？*
生成概率在 SCS 里表示：给定当前 backbone language model，对一条指令样本 `x` 的自然性、流畅性和分布内可生成性的估计。实现上不是整句概率连乘，而是 token-level conditional probability 的几何平均，也就是平均 log-likelihood：

```text
p(x) ≈ exp( 1/T * Σ log p(x_t | x_<t) )
```

它被用来刻画 intrinsic quality，主要假设是：高质量、自然、符合语言和指令数据分布的文本，通常会被模型赋予更高似然；低质量文本，比如语法错误、乱码、异常格式、非自然拼接、噪声 token，会让模型在逐 token 预测时更不确定，因此平均生成概率更低。这样 SCS 在聚合簇级概率时，会降低这些低质量样本对“多样性”的贡献，避免把噪声当成新信息。

这个假设会在几类情况下失效：

1. **高质量但低频/专业文本**  
   例如医学、法律、代码、少数语言或垂类业务指令。它们可能很有价值，但因为 backbone 预训练分布覆盖不足，生成概率偏低，被误伤。

2. **低质量但高频模板文本**  
   一些模板化、套话、重复问答、浅层客服话术可能非常容易被模型生成，概率高，但实际训练价值不高。

3. **模型自身偏见影响似然**  
   生成概率反映的是模型已学到的分布，不等于人类质量判断。如果模型偏好某些文风、语言、长度或格式，SCS 会继承这个偏好。

4. **任务目标和通用语言质量不一致**  
   如果目标是提升特定能力，比如数学推理、代码修复、安全拒答，文本的“自然可生成性”未必等于“训练有用性”。

5. **分布外但目标相关的数据**  
   新任务、新领域数据可能本来就和当前模型分布不同，生成概率低并不代表质量差，反而可能是模型需要补的能力。

所以我会把生成概率理解为 intrinsic quality 的一个弱代理，而不是绝对质量评分。它适合过滤明显异常和低质量文本，但需要和语义簇结构、中心距离权重一起使用；在垂类场景里最好换成同域 backbone，或加入领域质量模型、人类偏好模型、规则校验等辅助信号。

### *你使用样本到簇中心的距离作为空间权重。为什么“靠近簇中心”就更有语义凝聚性？有没有可能簇中心反而是不好的代表？*
“靠近簇中心更有语义凝聚性”的前提是：一个语义簇里的大多数样本共享某种主导意图或主题，簇中心可以近似表示这个局部语义模式的原型。样本离中心越近，说明它越接近该簇的共性语义；离中心越远，越可能是边界样本、混合意图样本，或者虽然被分进来了但和主簇语义不完全一致的外部分布离群点。

在 SCS 里，这个权重不是为了说中心样本“一定更有训练价值”，而是为了避免传统几何指标把稀疏边界点当成高多样性。也就是说，它是一种反离群机制：让簇内部核心语义承担更稳定的概率质量，让边缘点少一些影响。

但确实有可能簇中心不是好代表，主要有几种情况：

1. **簇本身不是单峰分布**  
   如果 HDBSCAN 把多个相近但不同的子意图合并成一个簇，均值中心可能落在两个语义团之间，反而不对应真实样本语义。

2. **embedding 空间不是线性语义空间**  
   直接对 embedding 求均值假设向量空间局部线性、欧氏距离有意义。但 LLM embedding 里距离和语义相似度不一定完全一致。

3. **簇内存在大量模板化样本**  
   如果簇中心由大量重复、模板化、低信息样本主导，那么靠近中心可能代表“常见”，不一定代表“有价值”。

4. **高价值长尾样本被误惩罚**  
   某些边界样本可能是真正有用的新能力、新任务形式或细粒度知识，不是噪声。中心距离权重可能把这类样本降权。

5. **簇大小或密度不均衡**  
   大簇的中心更稳定，小簇或稀疏簇的中心可能受单个样本影响很大。

所以更准确地说，中心距离权重是在“统计意义上”把簇内核心语义视为更凝聚，而不是绝对认为中心样本更好。面试时我会补一句：这个设计依赖聚类质量，因此论文里需要做 `min_cluster_size`、backbone、去掉 spatial weighting 的消融；工程上也可以用 medoid、局部密度、kNN cohesion 或多中心建模替代简单均值中心，来降低中心失真的风险。

### *请你从输入 ShareGPT 数据开始，完整讲一遍 SCS 的计算流程，包括 embedding、clustering、generation probability、cohesion weight 和最终 entropy。*
### *你代码里 `cal_gen_prob` 用的是 token log probability 的 mean。为什么不能直接用整句概率乘积？为什么 mean log-probability 更合适？*
### *HDBSCAN 输出的噪声点标签是 `-1`。你代码里把它们映射成一个新的语义类，而不是直接丢弃。这个设计合理吗？会不会让离群点仍然贡献熵？*
### *论文公式里空间权重是 `exp(-||e_i-e_0||^2)`，但代码里看起来是 `exp(-euclidean distance)`。这是有意设计还是实现差异？两者对远离中心的样本惩罚有什么不同？*

## 聚类与表示

### *为什么选择 UMAP + HDBSCAN，而不是 KMeans、GMM 或直接在原始 embedding 空间上聚类？*
选择 `UMAP + HDBSCAN` 主要是因为 SCS 需要的是“自适应发现语义模式”，而不是预先假设数据有固定类别数。

`UMAP` 的作用是先把高维 embedding 压到较低维，在尽量保留局部邻域结构的同时缓解高维空间里的距离退化问题。直接在原始 embedding 空间聚类，cosine/euclidean 距离容易受高维噪声影响，密度结构也不稳定，HDBSCAN 这类密度聚类会更难工作。

`HDBSCAN` 的优势是：

1. **不需要预设簇数**  
   指令数据的语义类别数本来未知。`KMeans` 和 `GMM` 都要先给定 `K`，这会把真实语义结构强行切成固定数量的簇。

2. **能处理非球形簇**  
   `KMeans` 隐含簇是凸的、近似球形，`GMM` 也依赖高斯分布假设。但指令语义空间里的簇形状可能很不规则。

3. **能识别噪声和边界点**  
   HDBSCAN 可以把低密度样本标成 noise，这和 SCS 想抑制离群点的目标一致。KMeans/GMM 往往会把每个点硬分配到某个簇，噪声也会被迫成为某个语义类的一部分。

4. **适合密度不均衡的数据**  
   指令数据中有些任务类型非常常见，有些很稀疏。HDBSCAN 对不同密度区域的建模通常比 KMeans 更自然。

不直接用 `KMeans/GMM` 的原因是，它们更适合“我要把数据切成 K 份”的选择任务，而 SCS 更关心“数据自己呈现出多少语义状态，以及每个状态的有效概率质量”。如果 K 设错了，熵会被人为放大或压缩，直接影响 SCS。

但这个选择也有代价：`UMAP` 和 `HDBSCAN` 都有超参数和随机性，比如 `n_neighbors`、`n_components`、`min_cluster_size`。所以论文里做了 `min_cluster_size` 的敏感性实验，默认取 30；代码里也固定了 UMAP `random_state=46` 来控制稳定性。实际工程里，如果数据规模非常大，我可能会考虑近似 kNN 图、MiniBatch KMeans + 二级聚类，或者用 HDBSCAN 的近似/分块方案来平衡效果和成本。

### *HDBSCAN 的 `min_cluster_size=30` 是怎么确定的？如果数据集规模从 1w 变成 100w，这个参数还应该固定为 30 吗？*
### *UMAP 降到 32 维会不会损失语义信息？你如何验证这个降维不会破坏后续聚类和 SCS 计算？*
### *你使用 mean pooling 得到样本 embedding。为什么不用 last token、CLS token、或者专门的 sentence embedding 模型？*
### *如果 embedder 和 generator 不是同一个模型，例如用 BGE 做 embedding、Llama 做生成概率，会带来什么问题？*

## 实验设计

### *你用 GPT-5.2 做 judge。LLM-as-a-judge 本身有偏差，这会不会影响 SCS 的有效性结论？你会怎么补充验证？*
会影响，而且这是一个必须正面承认的实验风险。

SCS 的有效性结论依赖“指标分数和下游模型性能的相关性”。如果下游性能本身是由 GPT-5.2 judge 给出的，而 judge 有系统性偏差，那么相关性可能部分反映的是“和 judge 偏好一致”，而不一定是真实用户偏好或真实任务能力。

可能的偏差包括：

1. **风格偏好**  
   judge 可能偏好更长、更礼貌、更结构化的回答。如果某类数据让模型更符合这种风格，SCS 相关性会被放大。

2. **模型家族偏好**  
   如果 judge 对某些表达、推理路径、拒答策略更友好，可能影响 Llama 和 Qwen 的评分公平性。

3. **benchmark 覆盖不足**  
   MT-Bench / AlpacaEval 更偏通用对话和指令遵循，不一定覆盖数学、代码、垂类知识、安全等能力。

4. **评价噪声**  
   LLM judge 对 prompt 顺序、答案长度、采样温度、rubric 表述都可能敏感。

我会这样补充验证：

1. **多 judge 交叉验证**  
   用不同强模型作为 judge，比如 GPT 系列、Claude、Gemini、开源 reward model，观察 SCS 相关性是否稳定。

2. **引入客观 benchmark**  
   加入有标准答案的任务，例如 MMLU、GSM8K、HumanEval、BBH、TruthfulQA、IFEval。这样可以减少纯主观 judge 偏差。

3. **人工评测小样本校准**  
   抽样让人工标注模型输出质量，比较 LLM judge 和 human preference 的一致性，再看 SCS 与人工评分的相关性。

4. **只看排序稳定性**  
   除 Pearson 外重视 Spearman。如果换 judge 后不同数据选择策略的排序仍稳定，说明结论更可靠。

5. **做 judge bias ablation**  
   控制回答长度、格式、语言风格后重新评测，检查 SCS 是否仍然和性能提升相关。

所以我的结论会比较谨慎：GPT-5.2 judge 可以作为高效、统一的自动评测工具，但不能单独证明 SCS 具有绝对有效性。更强的结论应该来自多评测器、多 benchmark、人工偏好校准下的一致相关性。

### *你说 SCS 和下游模型性能相关性达到 0.968 / 0.985。这个相关性具体是 Pearson 还是 Spearman？为什么论文里取两者平均？*
### *实验里下游性能是 MT-Bench 和 AlpacaEval 的归一化平均。为什么这样设计？如果两个 benchmark 偏好不同，相关性还能说明问题吗？*
### *你们比较了 Random、KMeans、K-Center、Repr Filter 四种数据选择策略。为什么选择这四类？它们分别代表了什么数据分布特性？*
### *NovelSum 在 Llama 上相关性 0.965，和 SCS 的 0.968 很接近。你会如何证明 SCS 的提升不是实验噪声？*

## 消融与鲁棒性

### *如果一个数据集类别非常均衡，但每个类别内部都是低质量样本，SCS 会给高分还是低分？为什么？*
理论上 SCS 不应该给特别高分，但它是否明显变低，要看这些低质量样本的生成概率在各类别之间是否都被压低，以及压低程度是否有差异。

SCS 最后算的是加权后的簇级分布熵：

```text
SC_k = Σ_{x_i in C_k} p(x_i) * w_i
P_k = SC_k / Σ_j SC_j
SCS = -Σ_k P_k log P_k
```

如果类别数量很均衡，但每个类别内部都是低质量样本，`p(x_i)` 会整体偏低。问题是：熵关心的是归一化后的分布形状，而不是绝对质量总量。如果所有类别都同等低质量，所有 `SC_k` 一起变小，归一化后 `P_k` 仍然可能接近均匀，那么 SCS 仍可能偏高。

所以严格说，SCS 能抑制的是“相对低质量、会改变有效语义分布的噪声”。如果整个数据集整体质量都差，但各簇质量差得很平均，单纯熵值可能无法充分反映“全局低质量”。

这也是一个可以被面试官追问的局限：SCS 不是纯质量指标，它是带有质量加权的多样性指标。为了处理这种场景，我会额外报告一个 quality mass，例如：

```text
Q = mean_i p(x_i)
```

或者同时输出：

- SCS：有效语义多样性
- 平均生成概率：整体内在质量
- 簇内平均 cohesion：语义凝聚程度
- 低概率样本占比：噪声比例

这样才能区分两种数据集：一个是“高质量且多样”，另一个是“低质量但均衡”。单看 SCS 熵，后者确实可能被高估。

### *如果一个数据集只有少数几个语义簇，但每个簇内部质量极高、非常凝聚，SCS 会不会低估它对某些任务的价值？*
### *去掉 spatial weighting 后相关性下降，去掉 probability 后也下降。你怎么解释这两个模块分别贡献了什么？*
### *如果生成概率模块使用的是 base model，而不是 fine-tuned model，会影响结果吗？论文中所谓 homologous advantage 是什么？*
### *SCS 随数据规模增长出现 plateau。这个现象从熵的角度如何解释？它对指令数据扩充有什么启发？*

## 工程实现与复杂度

### *你的 SCS 计算复杂度主要瓶颈在哪里？相比 NovelSum 为什么更快？*
### *代码里特征被写入 sqlite，embedding 用 pickle 存 blob。为什么这么设计？如果数据量达到千万级，你会怎么改？*
### *当前代码逐条计算 embedding 和 generation probability。如何做 batch 化优化？需要注意哪些 padding 和 loss mask 问题？*
### *`cal_gen_prob` 里对整个拼接后的 conversation 计算 likelihood。对于 ShareGPT 多轮对话，是否应该只计算 assistant response 的概率？为什么？*
### *如果线上要做大规模数据池筛选，你会如何把 SCS 改造成可扩展的 pipeline？比如分布式 embedding、近似聚类、增量更新怎么做？*

## 针对简历表述的追问

### *你简历里写“用于预测指令微调数据集对下游模型性能的贡献”。那 SCS 是因果指标还是相关指标？如果让我基于 SCS 自动选数据，你会怎么设计闭环实验？*
SCS 更准确地说是**相关指标**，不是严格因果指标。

它衡量的是一个数据集的“有效语义多样性状态”与下游微调性能之间的相关性。论文里用 Pearson / Spearman 相关性证明：SCS 分数高的数据集，通常对应更好的 MT-Bench / AlpacaEval 表现。但这不能说明“提高 SCS 本身必然因果导致模型性能提升”，因为中间还有很多混杂因素，比如数据质量、任务难度、领域覆盖、答案风格、长度分布、训练超参、benchmark 偏好等。

如果要基于 SCS 自动选数据，我会设计一个闭环实验，而不是只按 SCS 排序后宣称有效。

**闭环设计**

1. **构建候选数据池**  
   从原始 instruction pool 中清洗基础噪声，保留足够大的候选集，比如 ShareGPT / UltraChat / WizardLM 或业务垂类数据池。

2. **生成多组候选子集**  
   用不同策略采样出多个同规模数据集：Random、KMeans、K-Center、Repr Filter、quality-only、diversity-only、SCS-guided。所有子集规模、token 数、语言比例、长度分布尽量控制一致。

3. **SCS-guided selection**  
   不能简单选单样本 SCS 最高，因为 SCS 是数据集级指标。可以做贪心或分层选择：
   - 先聚类得到语义簇；
   - 在每个簇内按 `generation probability * cohesion weight` 排序；
   - 按目标簇分布选择样本；
   - 每轮加入候选 batch 后重新估计数据集 SCS；
   - 选择使 SCS 增益最大的 batch，同时设置质量下限，避免低质样本进入。

4. **训练验证闭环**  
   对每个候选子集独立 LoRA/SFT 微调，固定 backbone、epoch、lr、batch size、seed 数量、训练 token 数。然后在多个 benchmark 上评测，而不是只看一个 judge 分数。

5. **因果性增强实验**  
   做干预实验：在相同初始数据集上，只改变一个因素。
   - 加入高 SCS 增益样本 vs 低 SCS 增益样本；
   - 加入同簇中心样本 vs 边界离群样本；
   - 加入高生成概率样本 vs 低生成概率样本；
   - 保持数据量和长度分布一致。  
   如果高 SCS 干预稳定带来性能提升，才更接近因果证据。

6. **在线迭代**  
   每轮训练后用目标模型重新计算 embedding / probability，因为模型能力变化后，数据的相对质量和语义分布也会变化。然后进入下一轮选择。

我会把 SCS 当作“数据选择的代理目标”或“候选数据集评分器”，而不是最终因果真理。真正闭环要靠：SCS 选集、固定条件训练、多 benchmark 验证、反事实/干预对照，最后看性能是否稳定提升。

### *你写“抑制内部低质噪声与外部分布离群点”。请你结合公式说明，低质噪声和分布离群点分别在哪一步被降权。*
### *你写“统一比较 12 类多样性指标作为 baseline”。这些 baseline 中哪些是 lexical-based，哪些是 distance-based，哪些是 distribution-based？各自最大缺陷是什么？*
### *如果面试官质疑：“你这个方法只是把质量和多样性混在一起，不是纯多样性指标。”你怎么回应？*
### *如果字节内部有一个面向某垂类任务的数据池，比如电商客服或广告文案，SCS 应该如何适配？是否还应该追求全局语义熵最大？*

七、算法基础和大模型基础

### Transformer 的 self-attention 复杂度是多少？
### Multi-head attention 的作用是什么？
### RoPE 的原理是什么？
### KV Cache 的作用是什么？会占用多少显存？
### LoRA 的核心公式是什么？
### LoRA rank 怎么选？
### SFT、DPO、RLHF 的区别是什么？
### temperature、top-p、top-k 分别影响什么？
### 为什么评测时通常 temperature=0？
### beam search 和 greedy decoding 有什么区别？
### perplexity 是否能直接衡量 instruction following 能力？
### embedding 相似度常用 cosine，为什么不用欧氏距离？
### BM25 的核心思想是什么？
### cross-encoder 和 bi-encoder 的区别是什么？
### HDBSCAN 的基本思想是什么？